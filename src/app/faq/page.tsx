import { FaqContent } from "@/components/FaqContent";

export const metadata = {
  title: "FAQ",
  description:
    "Sizing, shipping, returns, and everything else you need to know before you shop BREAKOUT CULTURE.",
};

export default function FaqPage() {
  return <FaqContent />;
}
