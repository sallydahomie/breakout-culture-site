import { AboutContent } from "@/components/AboutContent";

export const metadata = {
  title: "About",
  description:
    "The mission behind BREAKOUT CULTURE: apparel built for founders, solopreneurs, and creators who chose the hard path over the safe one.",
};

export default function AboutPage() {
  return <AboutContent />;
}
