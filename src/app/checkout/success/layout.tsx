export const metadata = {
  title: "Order Confirmed",
  description: "Your BREAKOUT CULTURE order has been placed.",
};

export default function CheckoutSuccessLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
